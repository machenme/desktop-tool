[project]
name = "demo"
version = "0.1.0"
description = "Windows 桌面优化助手 — 清理图标缓存、规范快捷方式名称、回收公共桌面图标"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pyinstaller>=6.17.0",
    "pywebview>=6.1",
]
